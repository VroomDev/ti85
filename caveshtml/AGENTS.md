# caveshtml — agent notes

HTML/JS port of **Caves**, a TI-85 ZShell side-scroller (CENGINE + `.LVL` packs).

Read `.cursor/plans/` before implementing. Follow `.cursor/rules/`. Original behavior lives in `asm/` and `levels/`; treat those as source of truth until a decision in `.cursor/plans/decisions.md` says otherwise.

When you add or change product intent, put it in the plan files — not only in chat.
